# Sanmill no-refresh retained v4 preparation attempt 001

Date: 13 August 2026

Status: `invalidated_unlaunched_never_authorize`

This record closes the first seed-70 preparation without deleting or
rewriting it. The control directory is
`learned_ai/checkpoints/scaffolded/s_gen_v2_sanmill_refereed/managed-sanmill-no-refresh-retained-v4-seed70`.
It contains no authorization, no `segments` directory, no checkpoint, no
optimizer and no training log. The controller ledger contains only sequence
0, `managed_plan_published`; completed games and segments are both zero.

## Preserved identities

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `controller-events.jsonl` | 444 | `3b25b3f28a48352b54ac80a0f2b792aa1d8629ab7032ebd4438ad5e1360d8254` |
| `plan.json` | 2,869 | `e9bf7d917daf26f26dc805288ea5cf8086cae0b4abd9a0dee7b4eea41c03ddc7` |
| `technical-readiness.json` | 6,698 | `369b1850d4d2d9aff4d71619c4f73cb4a75e8f40a50ecee248af3e0e1e8dd5a9` |
| `preparation-disposition.json` | 1,303 | `aa53787dc2569d71213a0e189ef2fe338cf3385778f3beef3fda4a6fb4fb184e` |

The plan identity is
`2421fa96ab471af19b4134a1782953770e729ea40df894b62e2b345af9211a25`
at source commit `f1a8974ad2a9cf81ba50f53b3f65c96641b2f8e2`. The disposition
identity is
`c81ceebcb1a877d28c220cfe99e2ed3afbe97da591afefd9c782636441594b6c`.

## Why the preparation is closed

The persisted technical-readiness file was a manually assembled supporting
report. No version-controlled generator produced its schema, it did not
preserve the exact first-segment command as an artifact, and the raw preflight
JSON and its file hash were absent. The report therefore cannot be the sole
acceptance evidence required by the repository working agreement.

The older mill-bonus template also acquired an empty WAL and a non-empty SHM
sidecar before this preparation. The attempt-001 runtime database itself is a
closed, byte-identical 45,056-byte file with SHA-256
`5a5d8eb1df4184b1ed3581258ab2490f6b1320c7f9fd8a5322affeaf2cad540d`,
but its presence does not repair the incomplete readiness provenance.

Publishing the version-controlled readiness implementation and corrected
contract moves `dev` beyond the plan's frozen commit, so the managed launch
gate also rejects attempt 001 on source identity. These are independent,
fail-closed reasons never to authorize it.

## Successor boundary

Attempt 002 may reuse seed 70 because attempt 001 produced no game or learned
state. It must use a new experiment ID, plan ID, control directory and writable
database path. Its database source is the separately named closed snapshot
`data/specialist_db.no_refresh_retained_v4.attempt_002.template.sqlite`, not
the sidecar-bearing historical template. Its final ignored evidence must be
generated by `scripts/generate_managed_generalist_readiness.py` at the exact
clean published source.

This record is preparation evidence only. It is not training, held-out,
strength, promotion, publication or release evidence.
