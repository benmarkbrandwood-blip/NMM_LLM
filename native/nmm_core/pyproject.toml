[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "nmm_core"
version = "0.1.0"
description = "Rust-accelerated NMM game engine core"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "nmm_core"
